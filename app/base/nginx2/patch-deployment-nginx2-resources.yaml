apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx2
  namespace: web2
  annotations:
    kustomize.config.k8s.io/behavior: merge
    kubectl.kubernetes.io/last-applied-configuration: |
      {"apiVersion":"apps/v1","kind":"Deployment"}
    # This is a kustomize patch, not a complete resource
spec:
  template:
    spec:
      containers:
        - name: nginx2
          resources:
            limits:
              cpu: 50m
              memory: 64Mi
            requests:
              cpu: 10m
              memory: 32Mi
