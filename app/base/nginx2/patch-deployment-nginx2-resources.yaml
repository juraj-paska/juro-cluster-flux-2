apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx2
  namespace: web2
spec:
  selector:
    matchLabels:
      app: nginx2
  template:
    spec:
      containers:
        - name: nginx
          resources:
            limits:
              memory: 64Mi
            requests:
              memory: 32Mi
