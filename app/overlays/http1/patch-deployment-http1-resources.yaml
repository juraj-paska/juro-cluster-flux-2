spec:
  template:
    spec:
      $setElementOrder/containers:
      - name: http1-container
      containers:
      - name: http1-container
        resources:
          limits:
            memory: 64Mi
          requests:
            memory: 32Mi
